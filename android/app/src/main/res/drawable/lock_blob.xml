<?xml version="1.0" encoding="utf-8"?>
<!-- Stands in for the mic blob export on the lock screen.
     A rounded squircle with the brand gradient, matching lib/suniomax/widgets/sx_mic_blob.dart's
     Flutter fallback — the two are drawn separately because this side has no Flutter engine. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <corners android:radius="86dp"/>
    <gradient
        android:startColor="#5CA9E8"
        android:endColor="#2C6AA2"
        android:angle="315"/>
</shape>
