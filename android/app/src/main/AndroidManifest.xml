<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <!-- Needed so the boot/restart receiver can resume tracking while the app is not visible. -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION"/>

    <!-- The app is advertised through Facebook Ads, which attributes installs by advertising ID.
         Declared by hand rather than pulled in with an SDK: Play reads this permission off the
         uploaded bundle to decide whether to ask the advertising-ID questions in Data safety, and
         without it the store treats the app as not touching the ad ID at all. -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID"/>

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <!-- Android 14+ requires the type-specific permission alongside the service type. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION"/>

    <!-- Android 13+ : without this the ongoing service notification is silently hidden. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>

    <uses-feature android:name="android.hardware.location.gps" android:required="false"/>

    <application
        android:label="Circle 360"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <!-- Off on purpose: the URL must reach app_links and the splash, which decides
                 whether to show the invite screen. Left on, the platform hands the URL
                 straight to GoRouter and the splash never runs. -->
            <meta-data
              android:name="flutter_deeplinking_enabled"
              android:value="false"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Invite deeplink, e.g. loc360://invite?code=ABC123&from=Mom -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="loc360" android:host="invite"/>
            </intent-filter>

            <!-- Cashfree's return_url after the mandate sheet, e.g. loc360://payment?sub=…
                 The SDK hands the result back through its own callback, so nothing parses this
                 — it exists so the redirect resolves to something instead of dead-ending on
                 "no app can perform this action" at the end of a successful payment. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="loc360" android:host="payment"/>
            </intent-filter>

            <!-- App Links equivalent, e.g. https://loc360.app/invite/ABC123.
                 TODO: inert until https://loc360.app/.well-known/assetlinks.json is published
                 with this app's signing certificate fingerprint. Until then Android treats
                 these links as web links and never opens the app. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https" android:host="loc360.app" android:pathPrefix="/invite"/>
            </intent-filter>
        </activity>
        <service
            android:name=".LocationTrackingService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="location"/>

        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>

        <!-- Required by flutter_cashfree_pg_sdk. Without it the plugin never calls
             setSubscriptionCheckoutCallback, so the mandate sheet opens and then no result
             ever comes back — the app would sit on "Confirming your payment" forever.
             Typed as a boolean by aapt; do not quote it as a string resource. -->
        <meta-data
            android:name="cashfree_subscription_flow_enable"
            android:value="true" />

        <!-- Where FCM puts a notification message that names no channel of its own. Without it
             Android 8+ has no channel to post to and drops the notification without a trace.
             POST_NOTIFICATIONS is already declared above for the tracking service's ongoing
             notification, so push needs no permission of its own here. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="@string/default_notification_channel_id" />

        <!-- Facebook Ads conversion reporting.

             All four values are generated by build.gradle.kts from android/facebook.properties
             (gitignored, see facebook.properties.example). String resources rather than literals
             because an App ID is all digits, and a raw android:value of digits is parsed as an
             integer and overflows — the classic silent failure of this integration.

             AutoInitEnabled is false unless both credentials are present, which is what makes a
             fresh clone with no facebook.properties boot instead of throwing out of the SDK's
             initialising ContentProvider. When it is false the other three are inert. -->
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />
        <meta-data
            android:name="com.facebook.sdk.ClientToken"
            android:value="@string/facebook_client_token" />
        <meta-data
            android:name="com.facebook.sdk.AutoInitEnabled"
            android:value="${facebookAutoInit}" />
        <!-- Install and session events, which is what an ad click is attributed against. Without
             them the conversions the app reports have no install to attach to. -->
        <meta-data
            android:name="com.facebook.sdk.AutoLogAppEventsEnabled"
            android:value="${facebookAutoInit}" />
        <!-- The advertising ID, which is what Facebook actually matches an install against. Set
             explicitly because leaving it unset makes the SDK warn on every launch and pick its
             own default — and a value this app has already committed to in Play Data safety, via
             the AD_ID permission at the top of this file, should not be left to a default.

             Android only. iOS collection is gated by App Tracking Transparency and is driven from
             Dart once the user answers — see att_consent.dart. -->
        <meta-data
            android:name="com.facebook.sdk.AdvertiserIDCollectionEnabled"
            android:value="${facebookAutoInit}" />

        <!-- TODO: add `com.google.firebase.messaging.default_notification_icon` pointing at a
             white-silhouette drawable. Until one exists, Android 5.0+ falls back to the launcher
             icon and renders it as a solid white square in the status bar. Needs a design asset,
             not a code change. -->

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>

        <!-- Android 11+ package visibility. The Cashfree SDK resolves the installed UPI apps to
             show them in the mandate sheet; without this it can see none of them and the user
             is offered an empty list on an otherwise perfectly good phone. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="upi"/>
        </intent>

        <!-- One-Tap Call hands the number to the dialer. Without this the platform hides every
             dialer from us on Android 11+, and url_launcher can see nothing to open. -->
        <intent>
            <action android:name="android.intent.action.DIAL"/>
            <data android:scheme="tel"/>
        </intent>
    </queries>
</manifest>
